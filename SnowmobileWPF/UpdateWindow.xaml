<Window x:Class="SnowmobileWPF.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:SnowmobileWPF.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:UpdateViewModel}"
        Title="Update Subscriber" Height="740" Width="560"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}">
            <TextBlock Text="{Binding DisplayHeader}" 
                       Style="{StaticResource HeaderText}"
                       VerticalAlignment="Center" Margin="30,0"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Border Background="{StaticResource CardBackgroundBrush}" Margin="25,15" Padding="30" 
                    CornerRadius="12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Effect="{StaticResource CardShadow}">

                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="FIRST NAME" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="LAST NAME" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="PHONE NUMBER" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="EMAIL ADDRESS" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="STREET ADDRESS" Style="{StaticResource FieldLabel}"/>
                    <TextBox Text="{Binding Street, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="CITY" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding City, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="STATE" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding Region, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="COUNTRY" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding Country, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="POSTAL CODE" Style="{StaticResource FieldLabel}"/>
                            <TextBox Text="{Binding PostalCode, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>
                        </StackPanel>
                    </Grid>

                    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Margin="0,5,0,20"/>

                    <TextBlock Text="SUBSCRIBER STATUS" Style="{StaticResource FieldLabel}"/>
                    <Border Background="#F8FAFC" Padding="15" CornerRadius="6" BorderBrush="#F1F5F9" BorderThickness="1" Margin="0,5,0,20">
                        <UniformGrid Columns="2" Rows="2">
                            <CheckBox Content="Active" 
                                      IsChecked="{Binding Subscriber.Active, UpdateSourceTrigger=PropertyChanged}" 
                                      Margin="5"/>
                            <CheckBox Content="Contest" 
                                      IsChecked="{Binding Subscriber.Contest, UpdateSourceTrigger=PropertyChanged}" 
                                      Margin="5"/>
                            <CheckBox Content="Manual Mail" 
                                      IsChecked="{Binding Subscriber.ManualMail, UpdateSourceTrigger=PropertyChanged}" 
                                      Margin="5"/>
                            <CheckBox Content="Commercial" 
                                      IsChecked="{Binding Subscriber.Commercial, UpdateSourceTrigger=PropertyChanged}" 
                                      Margin="5"/>
                        </UniformGrid>
                    </Border>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Margin="0,0,12,0" 
                                Click="CancelButton_Click" IsCancel="True"/>
                        <Button Content="Save Changes" Style="{StaticResource PrimaryButton}" Width="160" 
                                Click="UpdateButton_Click" IsDefault="True"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</Window>