<Window x:Class="SnowmobileWPF.RenewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:SnowmobileWPF.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:RenewViewModel}"
        Title="Renew Subscription"
        Height="350" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}">
            <TextBlock Text="Renew Subscription" Style="{StaticResource HeaderText}" VerticalAlignment="Center" Margin="30,0"/>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}"
                Margin="25,15" Padding="25" CornerRadius="12"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Subscriber info (left) + Years picker (right) -->
                <Grid Grid.Row="0" VerticalAlignment="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="1"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left: context -->
                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                        <TextBlock Text="SUBSCRIBER" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding SubscriberName}" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{StaticResource PrimaryBrush}" TextWrapping="Wrap" Margin="0,0,0,12"/>
                        <TextBlock Text="CURRENT EXPIRY" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding CurrentExpiry}" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{StaticResource MutedTextBrush}"/>
                    </StackPanel>

                    <!-- Vertical divider -->
                    <Border Grid.Column="1" Background="{StaticResource BorderBrush}" Margin="0,4"/>

                    <!-- Right: years picker -->
                    <StackPanel Grid.Column="2" Margin="15,0,0,0">
                        <TextBlock Text="YEARS TO RENEW" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
                        <ComboBox ItemsSource="{Binding YearsSource}" SelectedValue="{Binding YearsToRenew}"/>
                    </StackPanel>
                </Grid>

                <!-- Cancel / Save -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Cancel" Style="{StaticResource SecondaryButton}" IsCancel="True"/>
                    <Button Grid.Column="2" Content="Save" Style="{StaticResource BlueButton}" IsDefault="True" Command="{Binding SaveCommand}"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
