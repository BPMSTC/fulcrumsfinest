<Window x:Class="SnowmobileWPF.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:SnowmobileWPF.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodels:SearchViewModel}"
        Title="Search Subscribers"
        Height="510" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}">
            <TextBlock Text="Search Filters" Style="{StaticResource HeaderText}" VerticalAlignment="Center" Margin="30,0,0,15"/>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" VerticalAlignment="Top"
                Margin="25,-20,25,0" Padding="25" CornerRadius="12" BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="1" Effect="{StaticResource CardShadow}">

            <StackPanel>
                <TextBlock Text="LAST NAME" Style="{StaticResource FieldLabel}"/>
                <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="FIRST NAME" Style="{StaticResource FieldLabel}"/>
                <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="PHONE NUMBER" Style="{StaticResource FieldLabel}"/>
                <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="VSCA" Style="{StaticResource FieldLabel}"/>
                <TextBox Text="{Binding VSCAText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="Cancel" Grid.Column="0" Style="{StaticResource SecondaryButton}" 
                            Click="CancelButton_Click" IsCancel="True"/>

                    <Button Grid.Column="2" 
                            Style="{StaticResource SearchButton}" 
                            Click="SearchButton_Click" 
                            IsDefault="True"
                            IsEnabled="{Binding CanSearch}">
                        <TextBlock Text="Search" Foreground="White"/>
                    </Button>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Window>