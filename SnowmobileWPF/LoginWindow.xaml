<Window x:Class="SnowmobileWPF.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" 
        Height="400" 
        Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="90"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}">
            <StackPanel VerticalAlignment="Center" Margin="0,0,0,15">
                <TextBlock Text="VSCA" 
                           Style="{StaticResource HeaderText}" 
                           FontSize="20" 
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Management System" 
                           Foreground="White" 
                           Opacity="0.7" 
                           FontSize="10" 
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" 
                Background="{StaticResource CardBackgroundBrush}" 
                VerticalAlignment="Top"
                Margin="30,-25,30,0" 
                Padding="25,20" 
                CornerRadius="10" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="1"
                Effect="{StaticResource CardShadow}">

            <StackPanel>
                <TextBlock Text="SERVER IP" Style="{StaticResource FieldLabel}"/>
                <TextBox Name="IPBox" Text="127.0.0.1:1433" Margin="0,0,0,10"/>

                <TextBlock Text="USERNAME" Style="{StaticResource FieldLabel}"/>
                <TextBox Name="UsernameBox" Margin="0,0,0,10"/>

                <TextBlock Text="PASSWORD" Style="{StaticResource FieldLabel}"/>
                <PasswordBox Name="PasswordBox" Height="34" Margin="0,0,0,20" VerticalContentAlignment="Center"/>

                <Button Name="LoginButton" 
                        Content="Login" 
                        Style="{StaticResource PrimaryButton}" 
                        Height="38"
                        HorizontalAlignment="Stretch"
                        IsDefault="True"/>

                <TextBlock Text="Contact Admin for access" 
                           HorizontalAlignment="Center" 
                           Margin="0,15,0,0" 
                           FontSize="10" 
                           Foreground="{StaticResource MutedTextBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>